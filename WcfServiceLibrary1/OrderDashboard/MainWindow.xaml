<Window x:Class="OrderDashboard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OrderDashboard"
        xmlns:controls="clr-namespace:OrderDashboard.Controls"
        Title="Order Dashboard" Height="400" Width="800">

    <Window.Resources>
        <local:BarWidthConverter x:Key="BarWidthConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button Content="Обновить данные"
                Padding="10,5"
                Margin="0,0,0,10"
                HorizontalAlignment="Left"
                Click="RefreshButton_Click"
                Grid.ColumnSpan="2"/>

        <!-- Бар-диаграмма -->
        <ItemsControl Grid.Row="1"
                      Grid.Column="0"
                      x:Name="ChartItems">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="0,2">
                        <TextBlock Text="{Binding Status}" Width="100"/>
                        <Border Background="SteelBlue"
                                Height="20"
                                Width="{Binding Count, Converter={StaticResource BarWidthConverter}}"/>
                        <TextBlock Text="{Binding Count}" Margin="5,0,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Круговая диаграмма -->
        <controls:PieChart x:Name="Pie" Grid.Row="1" Grid.Column="1"/>
    </Grid>
</Window>
